[pytest]
pythonpath = .

testpaths = tests

asyncio_mode = auto

markers =
   integration: mark tests as integration tests.
   unit: mark tests as unit tests.
   model: mark tests that load a real Whisper model (slow, GPU-bound, run serially).
